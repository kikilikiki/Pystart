# assets/

Ressources statiques livrees avec l'application (icones, captures d'ecran).

- `icon.ico` / `icon.png` : icone de l'application (a ajouter).
- `screenshots/` : captures utilisees dans le README.

Ces fichiers sont embarques dans le build PyInstaller si necessaire
(voir `scripts/build.py`).
